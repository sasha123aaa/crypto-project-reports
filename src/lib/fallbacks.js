export function getUsersFallbackText(project){
  if(project.projectType==="l1"||project.projectType==="l2"){
    return ["Пользовательские метрики важны для оценки реального спроса внутри сети.","Сейчас доступны только частичные данные: надежный актуальный источник подключается, поэтому показываем подтвержденные значения."];
  }
  return ["Для этого типа актива пользовательские метрики пока недоступны в стабильном актуальном источнике.","Блок показывает статус частичных данных без синтетических значений."];
}
