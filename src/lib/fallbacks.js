export function getUsersFallbackText(project){
  if(project.projectType==="l1"||project.projectType==="l2"){
    return ["Пользовательские метрики важны для оценки реального спроса внутри сети.","Сейчас блок работает в режиме partial: надежный live-источник подключается, поэтому показываем только подтвержденные данные."];
  }
  return ["Для этого типа актива пользовательские метрики пока недоступны в стабильном live-источнике.","Блок оставлен в честном partial-режиме без синтетических значений."];
}
